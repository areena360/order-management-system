name: SMTP secret check
on: [push, pull_request]
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject SMTP passwords in tracked configuration
        shell: pwsh
        run: ./scripts/check-smtp-secrets.ps1
